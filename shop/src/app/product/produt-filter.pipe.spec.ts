import { ProdutFilterPipe } from './produt-filter.pipe';

describe('ProdutFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProdutFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
